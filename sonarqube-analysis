node{
   stage("ACM checkout"){
       git 'https://github.com/joshimahesh/sampleapp'
    }
    stage("Compile-Package"){
       def mvnHome = tool name: 'maven-3', type: 'maven'
     sh "${mvnHome}/bin/mvn package"
    }
    
    stage('SonarQube analysis') {
     def mvnHome = tool name: 'maven-3', type: 'maven'
       withSonarQubeEnv('sonar-6') {
         sh "${mvnHome}/bin/mvn -P mvn sonar:sonar \
  -Dsonar.host.url=http://localhost:9000 \
  -Dsonar.login=a91334acbc296ae52012d52e66c516cf0e59a828"
       
       }
    }
    stage("Slack Notification")
    {
        slackSend baseUrl: 'https://hooks.slack.com/services/', 
        channel: '#jenkins-pipeline-demo',
        color: 'good', 
        message: 'Welcome to Jenkins!! Slack notification.',
        teamDomain: 'bitwise-demoworkspace', 
        tokenCredentialId: 'slack-demo'

   }
 
}
